#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{	
    char s[100];
    scanf("%[^\n]%*c", &s);
    //%[^\n]%*c is used to take input as a string with spaces
    printf("Hello, World!\n%s",s);
    return 0;
}

